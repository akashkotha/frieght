# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "cd backend/FreightERP.API",
  "dotnet restore",
  "dotnet publish -c Release -o out"
]

[start]
cmd = "cd backend/FreightERP.API/out && dotnet FreightERP.API.dll"
